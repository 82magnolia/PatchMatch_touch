%% Ablation on a 20-object subset of the full pipeline benchmark.
%% The refinement block scores the final prediction; "w/o neural refinement" is
%% the coarse transfer. Modality/field-of-view numbers (which score the coarse
%% transfer) are reported in the text.
\begin{table}[t]
  \centering
  \small
  \caption{\textbf{Ablation of the refinement stage} on a 20-object subset of the full pipeline
    benchmark. The first row is the coarse transfer, with no refinement.}
  \label{tab:ablation}
  \begin{tabular}{l c c c}
    \hline
    Variant & PSNR $\uparrow$ & SSIM $\uparrow$ & LPIPS $\downarrow$ \\
    \hline
    w/o neural refinement       & 27.71 & 0.8244 & 0.2797 \\
    w/o temporal FiLM           & 29.90 & 0.9022 & 0.1735 \\
    w/o normal concatenation    & 25.38 & 0.8540 & 0.2722 \\
    Ours (full)                 & \textbf{31.40} & \textbf{0.9074} & \textbf{0.1522} \\
    \hline
  \end{tabular}
\end{table}
