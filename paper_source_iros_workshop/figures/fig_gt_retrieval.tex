%% Qualitative results, ground-truth retrieval benchmark (two columns).
%% Graphic: figures/gt_retrieval_cropped.png (3 rows) -- rows are touch locations, columns are
%% ref touch | ref normal render | query normal render | coarse | ours | GT.
\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/gt_retrieval_cropped.png}
  \caption{\textbf{Qualitative results on the ground-truth retrieval benchmark.} Each row is one
    touch location. The normal renders cover four times the sensor footprint, and the red box marks
    the sensor's own patch. Tactile frames are the middle, roughly deepest-contact frame.}
  \label{fig:gt_retrieval}
\end{figure*}
